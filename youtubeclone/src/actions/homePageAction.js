import { createAction } from "@reduxjs/toolkit";

export const fetchHomePageData = createAction("FETCH_HOMEPAGE_DATA_REQUEST");
export const fetchSearchPageData = createAction(
  "FETCH_SEARCGPAGE_DATA_REQUEST"
);
